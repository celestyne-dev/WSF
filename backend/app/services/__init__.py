# Business logic that shouldn't live in route handlers: slug generation +
# reserved-slug/uniqueness validation, redirect creation on slug change,
# Cloudinary upload helpers, payment-provider abstraction (M-Pesa Daraja
# first, Stripe/PayPal pluggable later), and newsletter sending.
